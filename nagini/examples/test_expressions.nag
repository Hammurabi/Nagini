# Test file for extended expression system
# Tests function calls, typed parameters, and constructor calls

# Function with typed parameters (strict typing)
def add_strict(a: int, b: int) -> int:
    return a + b

# Function with untyped parameters (loose typing) but with return type
def add_loose(a, b) -> int:
    return a + b

# Function with *args
def sum_all(*args) -> int:
    total = 0
    # TODO: Implement loop over args
    return total

# Function with typed and untyped mix
def mixed_params(name: str, age, score: int) -> int:
    # name is strict (must be str)
    # age is loose (any type)
    # score is strict (must be int)
    return score

def main():
    # Test strict typed function
    result1 = add_strict(5, 3)
    print("Strict add result:", result1)
    
    # Test loose typed function
    result2 = add_loose(10, 20)
    print("Loose add result:", result2)
    
    # Test mixed parameters
    result3 = mixed_params("Alice", 25, 100)
    print("Mixed params result:", result3)
